import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 20,
    borderTopWidth: 1,
    borderTopColor: '#333333',
  },

  image: {
    width: 60,
    height: 60,
    alignSelf: 'center',
    marginTop: 20,
  },

  message: {
    fontWeight: '700',
    fontSize: 16,
    textAlign: 'center',
    color: '#808080',
    marginTop: 20,
  },
});
